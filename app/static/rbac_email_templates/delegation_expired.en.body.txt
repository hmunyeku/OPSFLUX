Hello,

{% if expiry_phase == 'j3' %}
The following delegation will expire in 3 days:
{% else %}
The following delegation has expired today:
{% endif %}

Delegator: {{ delegator.full_name }} ({{ delegator.email }})
Delegate: {{ delegate.full_name }} ({{ delegate.email }})
Period: {{ delegation.start_date }} -> {{ delegation.end_date }}
Permissions: {{ delegation.permissions | length }}

{% if expiry_phase == 'j3' %}
If a renewal is required, the delegator must create a new delegation. No automatic renewal (ISO 27001 §A.9.2.6).
{% else %}
The delegated permissions are no longer active. The RBAC audit trail preserves the record of this delegation.
{% endif %}

Certificate fingerprint: {{ content_hash | default('—') }}
