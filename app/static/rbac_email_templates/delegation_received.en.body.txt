Hello {{ delegate.full_name }},

{{ delegator.full_name }} ({{ delegator.email }}) has granted you a permissions delegation on your OpsFlux account.

Effective period: {{ delegation.start_date }} -> {{ delegation.end_date }}
Number of permissions received: {{ delegation.permissions | length }}
Reason: {{ delegation.reason }}

These permissions are now active on your account until {{ delegation.end_date }}.
The delegation certificate is attached to this email (PDF with SHA-256 fingerprint).

Certificate fingerprint: {{ content_hash | default('—') }}
