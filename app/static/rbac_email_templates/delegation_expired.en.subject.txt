{% if expiry_phase == 'j3' %}[OpsFlux] Delegation expires in 3 days — {{ delegator.full_name }} → {{ delegate.full_name }}{% else %}[OpsFlux] Delegation expired today — {{ delegator.full_name }} → {{ delegate.full_name }}{% endif %}
