Bonjour,

{% if expiry_phase == 'j3' %}
La délégation suivante expirera dans 3 jours :
{% else %}
La délégation suivante a expiré aujourd'hui :
{% endif %}

Délégant : {{ delegator.full_name }} ({{ delegator.email }})
Délégué : {{ delegate.full_name }} ({{ delegate.email }})
Période : {{ delegation.start_date }} -> {{ delegation.end_date }}
Permissions : {{ delegation.permissions | length }}

{% if expiry_phase == 'j3' %}
Si un renouvellement est nécessaire, le délégant doit créer une nouvelle délégation. Pas de renouvellement automatique (ISO 27001 §A.9.2.6).
{% else %}
Les permissions déléguées ne sont plus actives. Le journal d'audit RBAC conserve la trace de cette délégation.
{% endif %}

Empreinte du certificat : {{ content_hash | default('—') }}
