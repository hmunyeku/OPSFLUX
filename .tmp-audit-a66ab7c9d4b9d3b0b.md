Now I have all the information I need. Let me compile the comprehensive audit report:

## AUDIT STRUCTURÉ - MODULE PROJETS vs SPÉCIFICATION

### RÈGLE 1.1 : Projects comme source de vérité

**Status:** ✅ (Partiellement)

**Proof:**
- `/app/models/common.py` lines 1567-1578: Model `Project` défini avec tous les champs ORM
- `/app/api/routes/modules/projets.py` lines 179-270: Fonction `_sync_linked_planner_activities_for_project_task` synchronise tâches → Planner via `critical_fields` (pob_quota, dates, statut)
- `/apps/main/src/hooks/useProjets.ts` lines 153-157: Hook `invalidatePlannerViewsFromTask` force rafraîchissement des vues Planner après modification tâche

**Écart:**
- La synchronisation est réactive (basée sur changements de champs critiques) mais pas documentée comme "source de vérité" au niveau du code. Les commentaires sont génériques.
- Les événements `project.task.planner_sync_required` existent mais pas de logs explicites du "propriété" des données

**Effort:** M | **Type:** Backend/Frontend

---

### RÈGLE 1.2 : Création native OU import Gouti

**Status:** ✅

**Proof:**
- `/app/schemas/common.py` lines 1667-1684: `ProjectCreate` schema accepte création native (sans external_ref)
- `/apps/main/src/services/projetsService.ts` lines 332-398: Méthodes Gouti disponibles (`goutiStatus`, `goutiCatalog`, `goutiGetSelection`, `goutiSaveSelection`, `goutiSyncSelected`)
- `/app/api/routes/core/gouti_sync.py` lines 221-244: Fonction `_upsert_project_from_gouti` crée projets Gouti avec `external_ref = "gouti:<gouti_id>"`

**Écart:** Aucun

**Effort:** S | **Type:** Backend/Frontend

---

### RÈGLE 1.3 : Import Gouti avec sélection individuelle, auto-inclusion parents, marquage lecture-seule

**Status:** ✅ (Complet)

**Proof:**

**3.1 - Sélection individuelle des tâches:**
- `/app/api/routes/core/gouti_sync.py` lines 69-76: Schéma `ProjectSelection` + `TaskSelection` avec mode `"some"` + `task_ids: list[str]`
- `/apps/main/src/services/projetsService.ts` lines 388-398: Méthode `goutiSaveSelection` POST vers `/gouti/sync-selected`

**3.2 - Auto-inclusion des parents (spec §1.3):**
- `/app/api/routes/core/gouti_sync.py` lines 1165-1181: Code explicite de chaînage ancestral:
  ```python
  if mode == "some" and wanted_ids:
    ref_to_parent: dict[str, str | None] = {}
    for gt in tree_tasks:
      ref = str(gt.get("ref_ta") or gt.get("_id") or "")
      if ref:
        ref_to_parent[ref] = gt.get("_parent_ref")
    ancestors_to_add: set[str] = set()
    for wid in wanted_ids:
      current = ref_to_parent.get(wid)
      while current:
        ancestors_to_add.add(current)
        current = ref_to_parent.get(current)
  ```
  Ligne 1180: Log explicite "auto-including %d ancestor tasks"

**3.3 - Marquage lecture-seule (Gouti-owned fields):**
- `/apps/main/src/services/projetsService.ts` lines 622-625: Constante `GOUTI_OWNED_PROJECT_FIELDS` liste champs non-modifiables: `name`, `code`, `description`, `status`, `priority`, `progress`, `start_date`, `end_date`, `actual_end_date`, `budget`
- `/apps/main/src/services/projetsService.ts` lines 627-637: Fonction `isProjectFieldEditable` retourne `false` pour projets Gouti sur champs en liste

**Écart:** Aucun

**Effort:** S | **Type:** Backend/Frontend

---

### RÈGLE 1.4 : Association site/installation obligatoire et modifiable

**Status:** ⚠️ (Non appliquée)

**Proof:**
- `/app/models/common.py` line 1571: `asset_id: UUID | None` - field nullable
- `/app/schemas/common.py` line 1684: `asset_id: UUID | None = None` - schema accept None (optional)
- `/app/api/routes/modules/projets.py` line 330: Aucune validation `assert asset_id is not None` lors création

**Écart:**
- La spécification exige que `asset_id` soit **obligatoire** ("mandatory") mais le schéma et le modèle le rendent **optionnel**
- Aucune validation backend n'empêche création de projets sans site/installation
- L'UI permet création sans sélection d'asset_id (modifiable ✓ mais pas obligatoire ✗)

**Impact métier:** Risque de projets "orphelins" sans localisation géographique/opérationnelle

**Effort:** M | **Type:** Backend + Schema validation

---

### RÈGLE 1.5 : Synchronisation Planner avec bouton, toggle, notifications automatiques

**Status:** ✅ (Complet au niveau tâche, UI hard-codée)

**Proof:**

**5.1 - Bouton + toggle Planner:**
- `/apps/main/src/pages/projets/TaskDetailPanel.tsx` lines 264-275: Bouton PlannerActionButton avec toggle switch
  ```typescript
  <PanelActionButton
    variant={isLinkedToPlanner ? 'default' : 'primary'}
    icon={isLinkedToPlanner ? <X size={12} /> : <Send size={12} />}
    onClick={handleTogglePlannerLink}
  >
    {isLinkedToPlanner ? 'Retirer du Planner' : 'Planner'}
  </PanelActionButton>
  ```

**5.2 - Méthodes API (send/unlink):**
- `/apps/main/src/services/projetsService.ts` lines 432-438: `sendToPlanner` POST `/projects/{projectId}/send-to-planner`
- `/apps/main/src/services/projetsService.ts` lines 445-447: `unlinkTaskFromPlanner` DELETE endpoint
- `/app/api/routes/modules/projets.py` lines 2109-2186: Backend `send_tasks_to_planner` endpoint

**5.3 - Notifications automatiques (events):**
- `/app/api/routes/modules/projets.py` lines 179-270: Fonction `_sync_linked_planner_activities_for_project_task` émet événement `project.task.planner_sync_required` sur changements critiques

**Écart critique (UI/i18n):**
- `/apps/main/src/pages/projets/TaskDetailPanel.tsx` lines 14, 102: Import + initialisation `useTranslation()` existent
- **Mais** ligne 274 : Label bouton hard-codé FRANÇAIS: `'Retirer du Planner'` / `'Planner'` - **PAS d'appel `t()`**
- **Mais** lignes 206-214: Messages toast hard-codés français:
  - `"Tâche envoyée au Planner"` 
  - `"Tâche déjà liée au Planner"`
  - `"Tâche retirée du Planner"`
  - `"Erreur lors du retrait"`
  - **PAS de traduction `t('projets.planner.sent')` etc.**

**Effort:** S (Planner sync) + S (i18n fix) | **Type:** Frontend (labels + translations)

---

## RÉSUMÉ DES ÉCARTS CRITIQUES (Ranked by Business Impact)

| # | Gap | Règle | Severity | Effort | Fix |
|---|-----|-------|----------|--------|-----|
| 1 | `asset_id` optionnel (spec exige mandatory) | 1.4 | 🔴 High | M | Add schema validator, enforce at creation |
| 2 | UI Planner labels hard-codé français (non i18n) | 1.5 | 🟡 Medium | S | Wrap with `t()`, create translation keys |
| 3 | Toast messages hard-codé français | 1.5 | 🟡 Medium | S | Extract to i18n keys, use `toast({ title: t(...) })` |
| 4 | Projet-level Planner button documentation | 1.5 | 🟢 Low | S | Verify PlannerLinkModal wiring in detail panel |

---

## CONFORMITÉ GLOBALE

- **Règle 1.1 (Source de vérité):** ✅ Partiellement - synchronisation exists, documentation peut être améliorée
- **Règle 1.2 (Création native OU Gouti):** ✅ Complet
- **Règle 1.3 (Import Gouti + sélection + parents + read-only):** ✅ Complet - auto-inclusion parents code explicite ligne 1165-1181
- **Règle 1.4 (Asset_id obligatoire):** ⚠️ **NON CONFORME** - schema/model permettent optionnel
- **Règle 1.5 (Planner sync + button + auto-notif):** ✅ Fonctionnel mais ⚠️ i18n non appliqué

**Taux de conformité estimé:** 3.5/5 règles (70%)
